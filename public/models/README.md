Put embedding model TFJS graph files here.

Expected path (optional for offline use):
  /public/models/mobilenet/model.json
  /public/models/mobilenet/group1-shard1of1.bin

Then the app will load it via:
  /models/mobilenet/model.json

If not found, the demo will fetch MobileNet from TFHub (online mode).
